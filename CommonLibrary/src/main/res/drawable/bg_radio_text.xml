<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed -->
    <item android:drawable="@color/primary_orange" android:state_checked="true">
        <!-- <shape android:shape="rectangle">
             <solid android:color="@color/primary_orange" />
             <stroke android:width="1dp" android:color="@color/primary_orange" />
             <corners android:radius="@dimen/button_radius" />
         </shape>-->
    </item>
    <!-- Default -->
    <item android:drawable="@color/white_6" android:state_checked="false">
        <!--<shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <stroke android:width="1dp" android:color="@color/primary_orange" />
            <corners android:radius="@dimen/button_radius" />
        </shape>-->
    </item>
</selector>