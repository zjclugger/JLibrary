<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充颜色 -->
    <solid android:color="@color/light_black" />
    <stroke
        android:width="1dp"
        android:color="@color/transparent" />
    <!-- 圆角半径 -->
    <corners
        android:bottomLeftRadius="@dimen/radius_largest"
        android:bottomRightRadius="@dimen/radius_largest"
        android:topLeftRadius="@dimen/radius_largest"
        android:topRightRadius="@dimen/radius_largest" />
</shape>